package dev.folderion.core;

/**
 * Well-known media / document slots declared in a layout schema.
 */
public enum MediaKind {
    IMAGE_SET,
    AUDIO_FILE,
    VIDEO_FILE,
    MARKDOWN,
    JSON_DOC,
    URL_FILE,
    BLOB_FILE
}
